[model]
name = Events
label = Events
hidden = yes

[fields.title]
label = Title
type = string

[children]
model = event
order_by = -pub_date, title

[pagination]
enabled = yes
per_page = 10

[fields.body]
label = Body
type = markdown
